# Port Workspace

This directory is reserved for the new modern LBA2 port implementation.

Keep it separate from:

- `reference/` for imported upstream/reference material,
- `work/` for extracted artifacts and temporary analysis outputs,
- `docs/` for reports and reverse-engineering notes.
