# Common Agent Traps

- A stale ignored `port/hello-world/` directory can survive on disk after the real Phase 1 workspace is moved to `port/`. If Phase 1 docs claim the placeholder was replaced but the directory still exists locally, delete it instead of preserving the obsolete layout.
- `sidequest/` and `LM_TASKS/` are independent workstreams, not part of canonical memory pickup. Do not treat them as execution owners unless a prompt explicitly widens scope.
- `python3 tools/codex_memory.py context --include-history ...` still hides excluded durable history by default. If a task explicitly needs `sidequest/`, `LM_TASKS/`, LM/debug streams, or other noncanonical history, add `--include-excluded-history` instead of assuming the records disappeared.
- The checked-in memory files can lag behind the live worktree. Verify the code and `git status` before treating `docs/codex_memory/current_focus.md` or any subsystem pack as a statement about dirtiness, generated outputs, or active experiments.
- Some Codex worktrees can have a stale `.git` pointer even when the filesystem checkout is usable. If `git status` fails with `not a git repository` under `.codex/worktrees/...`, treat the tree as a plain filesystem checkout until the worktree metadata is repaired instead of assuming the repo content is missing.
- A `.codex/worktrees/...` checkout may not mirror bulky original-runtime artifacts under `work/`. If a helper needs extracted `LBA2.EXE` assets, verify the canonical path under `D:\repos\reverse\littlebigreversing\work\...` instead of assuming the current worktree carries the same files.
- `docs/PORTING_REPORT.md`, `docs/MEMSYS.md`, and older migration notes are historical context, not the current implementation contract. Use `docs/LBA2_ZIG_PORT_PLAN.md`, `docs/codex_memory/`, and the live `port/` tree for current state.
- The canonical current-state owners are `docs/LBA2_ZIG_PORT_PLAN.md`, `docs/codex_memory/current_focus.md`, and the relevant subsystem packs. Do not recreate a separate prompt-brief owner for the next slice.
- Raw `SCENE.HQR` entry bytes are not the runtime scene payload. The classic loader reads an HQR `COMPRESSED_HEADER` first and may LZ-decompress the entry before parsing scene fields, so typed decoders must decode the resource block before interpreting world/object/zone data.
- Scene numbering is easy to misread. The classic loader calls `Load_HQR(..., numscene + 1)` because `SCENE.HQR[0]` is reserved for `SizeCube.MAX`, while the current `inspect-scene` command accepts the raw one-based HQR entry index. Keep semantic scene numbers and HQR entry numbers separate.
- The raw-to-loader mapping has two offsets in this repo, not one. Classic code uses raw HQR slot `0` for `SizeCube.MAX`, while the Zig tooling exposes physical entries starting at `1`, so `SCENE.HQR[2]` is classic loader scene `0` and `classic_loader_scene_number` must use `entry_index - 2` for real scenes.
- `inspect-room-intelligence` no longer reads the legacy LBArchitect `.hqd` files at runtime. Room names now come from the checked-in generated Zig module at `port/src/generated/room_metadata.zig`, so update labels by rerunning `py -3 .\tools\generate_room_metadata.py` instead of patching the runtime resolver or reviving the old text-file parser.
- The MBN `SCENE.HQR` lists use type markers that matter. In post `177798`, `~1` means isometric room data and `~2` means outside scene data; keep the type marker with the descriptor instead of copying only the human-readable scene name.
- The checked-in MBN corpus index is broader than the raw evidence layer. A topic can appear in `docs/mbn_reference/corpus/index/topics_discovered.jsonl` and `topic_classification.jsonl` without a matching raw post payload or any `tools/mbn_workbench.py search-evidence` hits.
- The shell-detection snippet in `AGENTS.md` is misleading under Bash because it uses PowerShell redirection syntax on the Bash side. Treat the current shell as Bash from the provided environment context or use a Bash-safe probe instead.
- Parked nested Git metadata under `reference/discourse-downloader/.git.disabled/` and `reference/littlebigreversing/.git.disabled/` is currently tracked by the parent repo. Treat those `objects/`, `refs/`, `logs/`, `HEAD`, and `config` files as portability noise, not as canonical project state that needs to be synchronized across machines.
- On this Windows checkout, `bash -lc 'git status'` under `/mnt/d/...` can report a false-positive mass-dirty worktree while native Windows `git status` shows only the real local edits. For repo-state or portability checks, trust native Git from the provided PowerShell shell before assuming hundreds of files changed.
- In the PowerShell-hosted workflow, `bash -lc "..."` can still be mangled before Bash sees it when the payload contains nested double quotes or PowerShell-sensitive tokens. Prefer single-quoted Bash payloads in tool calls so the inner command reaches Bash unchanged.
- On the current Windows setup, reusing the checked-in Ghidra project from a `.codex/worktrees/...` path is not a reliable `ghb` spike target. Ghidra can report `project locked` or `IllegalArgumentException` path-element errors there, so prefer a disposable project outside the Codex worktree for fresh static-analysis automation.
- On the Windows-first build path, `bash -lc` is not a reliable wrapper for canonical Zig verification. Use `py -3 .\scripts\dev-shell.py exec --cwd port -- zig build ...` or open `py -3 .\scripts\dev-shell.py shell` from native PowerShell for real validation.
- The tiny top-level Zig test roots under `port/src/` are intentional. If you point `build.zig` test shards directly at files under `src/tools/` or `src/game_data/...`, Zig can fail with `import of file outside module path` when those tests reach back across the `src/` tree, so keep the wrapper roots unless you also redesign the module layout.
- `scripts/verify_viewer.py --fast` is not a cheap smoke test. It already runs `zig build test-fast`, `zig build test-cli-integration`, `zig build stage-viewer`, tool probes, and live viewer launches, and the guarded `2/2` launch satisfies the verifier's own success predicate after about 14 seconds when run in isolation. If `verify_viewer.py --fast` times out while other heavy Zig builds are running in parallel or under a tight outer command timeout, treat that as orchestration contention first, not as proof that `2/2` viewer startup regressed.
- On the current Windows setup, `bash -lc python3 ...` works for repo scripts but that interpreter does not have `pip`. Use native PowerShell `py -3 -m pip install -r requirements.txt` for Python dependency installs, then keep using `python3` or `py -3` for the actual script/test commands as needed.
- In native PowerShell, piping `zig build ...` through `Out-String` can still misreport a successful build as exit code `-1`. Keep scripted acceptance on `scripts/verify_viewer.py` plus `scripts/dev-shell.py exec` instead of rebuilding native-command capture in ad hoc PowerShell.
- Windows `vcvars` imports can report the toolchain path under `Path` instead of `PATH`. Keep `scripts/dev-shell.py` normalizing environment keys case-insensitively before looking up tools like `cl`, `cmake`, and `zig`.
- The guarded runtime/load seam is now wider than the old `19/19` baseline. `19/19`, `2/2`, and `11/10` are guarded-positive room/load pairs; `44/2` is the explicit guarded exterior rejection. Do not revive the old unsupported-life rejection story for `2/2` or `11/10`.
- The guarded `19/19` viewer zone summary intentionally splits presentation. HUD copy uses `ZONES NONE` / `ZONES <indices>`, while stderr diagnostics use structured `zones=none` / `zones=<indices>`. Keep both driven from runtime-owned zone membership instead of collapsing them into one invented current-zone label.
- The canonical `SCENE.HQR[2]` / `LBA_BKG.HQR[2]` pairing is not the whole interior background loader story. Classic interior loading also depends on `LBA_BKG.HQR[0]` as `T_BKG_HEADER` plus a late `TabAllCube` entry at `BkgHeader.Brk_Start + BkgHeader.Max_Brk`.
- `port/src/assets/hqr.zig` now has two HQR index spaces on purpose. The older helpers follow the repo's one-based view that skips classic slot `0`, while the classic helpers use the engine's zero-based `Load_HQR` numbering. Use the classic helpers for `LBA_BKG.HQR[0]` and other loader-faithful paths.
- `zig build test` is a real-asset regression gate, not a hermetic unit-test-only pass, and it does not prove the executable targets compile. Keep explicit `zig build run` or `zig build tool` acceptance commands when runtime code changes.
- `zig build test-fast` used to stall in the same-index decoded-interior triage path inside `tools/cli.zig`. The bounded synthetic triage assertions now stay in `test-fast`, the slower asset-backed room/load checks live in `zig build test-cli-integration`, and the heavy same-index triage workload is tool-only via `zig build tool -- triage-same-index-decoded-interior-candidates --json`. If `test-fast` stalls again, check whether the heavy asset-backed triage path was reintroduced into the test graph before debugging runtime or asset loaders.
- Tool-only CLI paths can stay half-wired if you only trust `zig build test-fast` or `zig build test`. A new subcommand can still be missing enum/parse/run plumbing, or formatter helpers can still be unreachable, until you execute the real `zig build tool -- <command> ...` path.
- `inspect-room-intelligence` does not treat `--out stdout` as a stream alias. The current writer passes `--out` straight to `createFileAbsolute`, so `zig build tool -- inspect-room-intelligence ... --out stdout` panics on Windows; omit `--out` for stdout or pass an actual absolute output path.
- `inspect-room-intelligence --out` currently expects an absolute path on Windows. Passing a relative path (for example `--out ..\work\room.json`) panics in `paths.makePathAbsolute` instead of returning a structured CLI error; use an absolute output path until the writer is hardened.
- Interrupted viewer launches can leave `port/zig-out/bin/lba2.exe` locked by a stale `lba2.exe` process. If the next install step fails with `AccessDenied`, clear the stale process before treating it as a code regression.
- Rich guarded `19/19` diagnostics do not mean Phase 5 has a playable-path candidate yet. The supported startup still lands on `raw_invalid_start`, and the current scene metadata reports `track_count=0`, so future Phase 5 planning must not mistake the diagnostic baseline for a gameplay-ready slice.
- Do not treat every guarded-positive `raw_invalid_start` as the same failure anymore. The current diagnostics now distinguish `19/19` (`best_alt_mapping=dense_swapped_axes_64`), `2/2` (`best_alt_mapping=swapped_axes_512_control`), and `11/10` (`best_alt_mapping=none`); use that split before choosing the next runtime-facing slice.
- The new decoded-interior ranking surface is offline-only. `rank-decoded-interior-candidates` now puts `SCENE.HQR[101]` first and `SCENE.HQR[19]` at `146/147`, but `inspect-room 219 219` still fails with `InvalidFragmentZoneBounds`, so do not equate the ranking winner with a runtime-admitted scene.
- The `219/219` blocker explanation now lives on `inspect-room`'s failure path. Before adding another blocker-only CLI or prompting for a new explanation surface, check whether stderr already prints `reason=invalid_fragment_zone_bounds` plus `event=fragment_zone_validation_issue ...` lines for the failing pair.
- `triage-same-index-decoded-interior-candidates` can return a technically compatible pair that says nothing about fragment-bearing room admission. On current assets, `86/86` is the highest-ranked compatible same-index pair above baseline, but it has `fragment_count=0` and `grm_zone_count=0`; `187/187` is the first compatible pair with actual fragment-zone data.
- `tools/codex_memory.py` validates the whole v2 tree, including subsystem-pack byte budgets, before it appends history. If an `add-*` command fails unexpectedly, check pack budgets and JSONL canonicalization, not just the new record.
- `docs/codex_memory/*.jsonl` are durable append-only history, not editable ledgers. If you change an old `task_events.jsonl` row in place, validation now rejects the file relative to `HEAD`; restore the historical row and append a new event instead.
- Memory history lookup now treats repo-relative `evidence_refs` as real retrieval edges too: exact and prefix path queries can match them, and subsystem-scoped pickup can infer ownership from them. If a record is still missing, check the query path string and normalization before assuming evidence-only history is ignored.
- `scripts/dev-shell.py shell` launches a configured child PowerShell; it does not mutate the parent shell in place. For scripted validation, prefer `scripts/dev-shell.py exec --cwd port -- ...` so the intended environment and working directory are explicit.
- Historical LM/debug notes and older typed history can still mention `scripts/trace-life.ps1`, but the canonical original-runtime tracer entrypoint is now `py -3 .\tools\life_trace\trace_life.py`. Do not reintroduce a wrapper layer unless the Python CLI is missing a required current-state behavior.
- `tools/life_trace/trace_life.py` now splits original-runtime lanes by proof seam and owner. `tavern-trace` is the canonical live Frida/FRA lane, `scene11-pair` is the canonical debugger-backed `cdb-agent` lane, `scene11-live-pair` is experimental only, and `basic` remains the direct-Frida fallback.
- DXWnd-managed windowed startup is acceptable on the Tavern proof lane. The canonical startup path now uses the single-slot `Load Game` harness with a staged save from `work/saves`; do not route Tavern or Scene11 back through `current.lba` or the old `Resume Game` path.
- On the structured `fra` life-trace lane, owned `--launch` runs now try `fra target terminate` first and then fall back to a direct kill if the process is still alive after a short grace window. If a launched Tavern run still leaves `LBA2.EXE` behind after the final cleanup status, treat that as abnormal.
- A successful structured Tavern run can still hit a transient `fra target terminate` timeout during teardown even when `fra daemon status` is healthy immediately afterward and the target registry ends in `terminated`. Treat that as a teardown-path issue, not as proof that the trace or refactor failed.
- On the Tavern late-attach proof lane, rich per-hit Frida reads were not worth carrying forward. The canonical stable path is the slim proof-bearing probe: fingerprint match, `0x76 @ 4883`, and post-`0x76` `loop_reentry @ 4884`, plus required screenshots. Re-expanding Tavern to hot-path `ptr_window`, `working_*`, or `exe_switch` reads risks intermittent `Application Error` crashes after the fingerprint match.
- `tools/life_trace/agent.js` refactors need a real Tavern acceptance run, not just `tools/test_life_trace.py`. The split-fragment agent passed Python regressions but regressed live Tavern proof until the Tavern scene stopped using the generic `DoLife` loop wrapper and installed its own known-good hook topology.
- `tools/life_trace/trace_life.py` now writes run bundles under `work/life_trace/runs/<run-id>/`. Debug runtime behavior from `raw.jsonl`; use `enriched.jsonl` only for host-side joins like helper-callsite attribution.
- `tools/life_trace/agent.js` now depends on a tracked fragment set under `tools/life_trace/agent/`, and `load_agent_source()` fails fast if any fragment is missing or if a placeholder survives assembly. Treat that as repo damage, not as a runtime oddity to work around.
- The canonical original-runtime `SAVE` baseline is now `current.lba` plus `SHOOT/` only. For controlled Tavern or Scene11 launches, stage exactly one additional `.lba` from `work/saves`, use `Load Game` on the sole visible slot, and then delete that staged save after the run. Do not route scene loads back through `current.lba` or the old `CURRENT` menu path.
- On the Sendell's Ball room automation lane, the behavior and inventory menus are press-and-hold surfaces. Tapping Left Ctrl or Left Shift can still produce a healthy-looking run log while the screenshot stays in the room, so keep those captures on a held-key path.
- On the Sendell direct-read lane, the obvious Ghidra `ListVarGame` symbol is a trap. `DAT_00499E96` is a two-byte pre-base used in high-word decompilation patterns, so the real `ListVarGame[0]` base is `0x00499E98`; otherwise `FLAG_BOULE_SENDELL` will look falsely nonzero before the room-36 event.
- For structured original-runtime evidence, raw `raw.jsonl` / `enriched.jsonl` bundles are not the first comparison surface anymore. Prefer typed lane summaries such as `scene11_summary.json` and `sendell_summary.json`, and only drop to the JSONL streams when the summary omits a field you actually need.
- On the current Sendell room-36 proof lane, direct `CurrentDial` reads are a trap if you treat them as dialog-step progression. The checked-in `0x00475630` read stays at `513` from `loaded_pre_cast` through `post_dialog_room` while `TypeAnswer` / `Value` and screenshots still show the later visible steps, so do not infer `513 -> 514 -> 287` from `CurrentDial` alone.
- On the current Sendell room-36 proof lane, `Value` is not a stable final-phase oracle by itself. Two successful reruns kept the same `post_dialog_room` `PtrPrg` and `PtrPrgWindow` signature (`0x...22FA` / `00 00 14 04 00 FF FF 00 00 00 67 00 00 13 80 05`) while `Value` differed (`12` vs `5`), so final-phase claims should use the whole signature plus screenshots and story-state deltas, not `Value` alone.
- A stable no-Frida single-slot `Load Game` control run still does not imply the Frida lane is healthy. On `2026-04-11`, `02-voisin.LBA` loaded through the single visible `Load Game` slot and stayed responsive for at least 30 seconds with no attach, while multiple Scene11 probe variants still froze after attach.
- The old blanket rule about `DoLifeEntry` freezing Scene11 is too broad. Under the corrected mounted-ISO single-slot `Load Game` path, no-op late attach, bare `DoLifeLoop`, paused debugger reads, and minimal `DoLifeEntry` probes have all been observed as safe. The unstable boundary is the broader live Scene11 seam, especially the full challenger lane after attach.
- For original-runtime cross-lane experiments, treat stray `LBA2.EXE` and `cdb.exe` processes as hard preflight cleanup, not as harmless leftovers. The `2026-04-13` crossing tests were only interpretable after forcing a clean process slate before each run. That does not prove stale processes caused every earlier failure, but it does remove a major source of false conclusions.
- `scene11-pair` is the canonical debugger-backed Scene11 owner. Do not revive the deleted `agent/scene_scene11.js` path or treat `scene11-live-pair` as interchangeable with it without new evidence.
- The stable Scene11 result is a runtime mismatch, not a startup failure. The loaded `02-voisin.LBA` room is reproducible, canonical objects `12` and `18` can be null, and runtime discovery can surface `LM_DEFAULT (0x74)` and `LM_END_SWITCH (0x76)` on live object `2`.
- That Scene11 runtime-owner discovery is state-sensitive. On the current save, the useful discriminator is whether the snapshot catches non-null `global_ptr_prg`.
- The save corpus now has more than one practical Neighbour's House candidate, but `scene11-pair` still defaults to `02-voisin.LBA`. When Scene11 ownership questions come up, compare bundle-backed `scene11_summary.json` outputs across saves instead of treating the default save as the only current-state truth.
- "Scene11" is not a sufficient operator instruction for save generation. Use `work/saves/save_profiles.json` and ask for a concrete generation profile such as `scene11-early-house`, `scene11-wizard-house`, or `scene11-returned-house`; Neighbour's House evidence is room plus progression context, not room alone.
- Do not outsource save-profile visual validation to automation or the operator alone. Load the save, capture the loaded-scene screenshot, and have Codex compare it against the profile's screenshot-verifiable cues here before trusting the save as proof input.
- Cross-model experiments proved portability, not interchangeability. A Tavern-style live seam can work on Scene11, and a Scene11-style snapshot can work on Tavern, but the canonical split stays in place because Tavern proves live behavior while Scene11 proves ownership/mismatch.
- `scene11-live-pair` remains experimental. Fresh repo-owned runs can still fail immediately after attach with `scene11_live_pair_application_error`, so do not reopen the canonical-lane decision from that challenger alone.
- Owned `trace_life.py --launch` runs now hard-kill any pre-existing `LBA2.EXE` and `cdb.exe` before launch, and that policy is intentional. It is not a root-cause fix for Scene11, but it is now the canonical launcher hygiene for original-runtime runs.
- The new preflight path initially broke live runs because `taskkill` returns localized text through a mojibaked codepage path. Matching the exact accented “no se encontró” string was too brittle; the durable marker is the partial stem `no se encontr`. Keep locale handling loose for Windows shell diagnostics.
- The runtime now treats an `Application Error` top-level window as an explicit failure signal instead of letting owned runs drift into timeouts or teardown confusion. After shipping that change on `2026-04-13`, the canonical Tavern rerun still completed `tavern_trace_complete` in [life-trace-20260413-014226/enriched.jsonl](/D:/repos/reverse/littlebigreversing/work/life_trace/runs/life-trace-20260413-014226/enriched.jsonl), and the canonical Scene11 debugger lane still reached the expected `scene11_static_runtime_mismatch` verdict in [life-trace-20260413-014318/enriched.jsonl](/D:/repos/reverse/littlebigreversing/work/life_trace/runs/life-trace-20260413-014318/enriched.jsonl).
- Removing FRA did not rescue the live Scene11 challenger. The remaining instability belongs to the Scene11 live attach seam itself, not just the transport.
- The clean counterexample still matters: Tavern continues to reprove on the canonical live lane, so when a Scene11 challenger and Tavern disagree, trust the canonical split first.
- The debugger-backed Scene11 owner now goes through `cdb-agent`, not raw `cdb.exe` calls from `littlebigreversing`. The stable seam is still `Scene11SnapshotReader` in `scene11.py`, while `life_trace_debugger.py` owns the transport. Keep higher-level Scene11 proof logic ignorant of the bespoke CLI and change the adapter, not the lane.
- `cdb-agent` is already validated against this repo without touching its worktree. On `2026-04-13`, `py -3 -m unittest discover -s tests -p "test_*.py"` passed in `D:\repos\cdb-agent`, `cdb-agent --json doctor` passed, and `py -3 .\scripts\validate_littlebigreversing.py --phase static` reported `repo_changed: false`. If the debugger lane breaks again, inspect the adapter boundary first, not the Scene11 snapshot contract.
- The canonical Scene11 snapshot adapter is `cdb-agent` one-shot `--pid` reads, not `debug bootstrap` remote sessions. `life_trace_debugger.py` intentionally stays on non-invasive reads; do not swap repo automation over to `request exec --session` without a new checked-in policy change.
- `cdb-agent request exec --session` is not a general-purpose repo automation surface here. The `2026-04-14` crash window hit one remote session with three simultaneous clients (`r eip`, `u 420574 L20`, and `? poi(4976d0)`) while the target was still at the initial debugger break, and the shared log showed interleaved WoW64 state changes plus broken remote-client exit handling. `cdb-agent` now serializes saved-session requests and avoids the old `; .remote_exit` pattern, but this repo should still treat detached remote sessions as single-client manual tooling and keep canonical Scene11 automation on one-shot `--pid` reads.
- The copied minidump `work/crash/041426-11031-01.dmp` sharpened that trap: the actual bugcheck was `SYSTEM_SERVICE_EXCEPTION (0x3b)` in `amdkmdag.sys`, and the owning process on the faulting thread was `msedgewebview2`, not `cdb.exe` or `LBA2.EXE`. Treat detached-session stepping from the Codex desktop app as a higher-risk GPU/UI path; if you must use `debug bootstrap` + `request exec --session`, do it from one plain terminal or one interactive WinDbg client, never parallel desktop-tool calls.
- The local one-shot `cdb-agent --pid --wow64` path is fixed again as of `2026-04-14`. The real bug was not Scene11 state; the Store `cdb.exe` path choked on `.effmach x86` when `cdb-agent` sent local WoW64 commands through `-c "..."`, and the parser also dropped prompt-prefixed rows like `0:000:x86> 004976d0 ...`. The current canonical adapter now drives local WoW64 commands through stdin and parses prompt-prefixed output correctly. If local Scene11 debugger reads regress again, inspect `cdb-agent` first before blaming the save or lane.
- `SCENE.HQR` giver `Info0` is a bonus-flag mask that `WhichBonus()` resolves later from runtime state, so a typed decoder should expose allowed kinds or preserve the raw mask instead of inventing one singular `bonus_kind`.
- `SCENE.HQR[19]` zone bounds are trigger volumes, not admitted floor-truth mapping anchors. Do not invent runtime mapping policy from camera or zone geometry without new checked-in evidence.
- On the guarded `19/19` locomotion seam, the exact containing-zone membership for the admitted `39/6` fixture and the accepted south step is the empty set. Treat that as the checked-in truth for now; do not invent a singular current zone or a different coordinate scale to force a non-empty answer.
- The guarded `19/19` admitted footing intentionally splits presentation. HUD copy uses `SURF ...`, while stderr diagnostics use `current_footing=...`; both come directly from runtime-owned `local_topology.origin_surface` plus `origin_standability`, and they stay absent on raw-start / `origin_invalid`.
- The old guarded unsupported-life load diagnostics are historical now for `2/2` and `11/10`. If you still see `ViewerUnsupportedSceneLife` for those pairs, treat it as a regression against the current Branch-A boundary, not as expected behavior.
- On the `waypoint_step_probe.py` collision lane, burst classification is the canonical blocked/moved oracle and the collision observer is diagnostic-only. Do not treat collision observer output as a direct blocked verdict; the useful interpretation is whether a pin persisted (`persistent_collision_pin`) or a transient pin was followed by movement (`transient_pin_then_escape`).
- The decoded scene object array is intentionally shorter than `object_count`. The classic header count includes the reserved hero slot, so use `scene.objects.len` for actual object iteration and render sizing.
- `runtime/room_state.zig` intentionally strips scene objects down to render-facing position snapshots unless a feature adds an explicit seed surface. If runtime work needs track or life payloads, use `scene.object_behavior_seeds` or add another explicit immutable snapshot field instead of assuming `scene.objects` still carry raw script bytes.
- `runtime/room_state.zig` also strips `SceneZone` semantics and the hero's decoded track/life programs down to bounds and byte-length metadata. Runtime locomotion can still report zone membership, but gameplay work like change-cube, giver, message, rail, or hero-script execution cannot be added from `RoomSnapshot` alone without widening that immutable snapshot contract.
- Decoded `change_cube` `destination_x` / `destination_y` / `destination_z` are not the final hero landing point for zone-triggered transitions. Classic `GereZoneChangeCube()` carries the hero's local offset from `zone.x0/y0/z0` through the zone transform and only `LM_CHANGE_CUBE` uses the saved start-position path, so do not wire runtime room transitions straight to the raw zone destination fields or collapse provisional zone-relative points into final landing points.
- The current guarded post-load adjustment is deliberately narrower than the full classic handoff path: for provisional zone-relative destinations it only resolves Y from the destination cell's top surface when the carried-over X/Z already land in an occupied standable cell. If X/Z map to an empty or out-of-bounds cell, the port still rejects with unsupported post-load adjustment; do not misread that as proof that classic would never salvage the landing through a wider collision path.
- Do not treat guarded `2/2` as a synonym for every playable Twinsen-house seam. Asset-side `inspect-room-intelligence` currently shows `2/2` as a very small room surface with one `change_cube` seam (the public exterior door) plus `message`/`scenario`/`giver` zones; if a classic house path involves locked-door, key, or object-script logic, prove its source room before assuming it lives in `2/2`.
- The guarded `2/2` public house exit is not a blocked interior-room handoff. Original-runtime slot-`0` tracing from the playable New Game lane captured a discontinuous jump from house coordinates to an exterior landing near `(18434,250,5702)`, so do not keep modeling `destination_cube=0` there as an interior same-index room candidate or as a post-load-adjustment problem.
- `FUN_004409AC` is a trap if you treat it as the ownership point for the public-house `FlagChgCube` pulse. The exact-seam Frida + `cdb` run showed `NewCube=0` while observed pulse writes include the tighter `LBA2+0x11AEB` / `0x11B2F` block, and direct Frida instruction hooks on those hot write sites were crash-prone. Keep the safe evidence stack on function-level Frida hooks plus `cdb` write breakpoints instead of reviving JS instruction hooks or attributing the pulse to the outer `004409AC` frame.
- The `work/tmp_house_*` save-lane artifacts are not all from the same watcher revision. `work/tmp_house_right_probe.py` now names the corrected candidates `obj_xyz_live` / `obj_xyz_minus4` / `obj_xyz_plus2`, but `work/tmp_house_right_probe_01-house_summary.json` still uses the older `obj_xyz_a` / `obj_xyz_b` labels. Do not mix that stale right-probe artifact into claims about the corrected methodology without rerunning the probe.
- Guarded `2/2` does expose typed `change_cube` semantics, but the current admitted locomotion footing does not yet provide a checked-in step into that zone. If a runtime test needs `2/2` room-transition proof today, drive it from direct zone membership instead of inventing a reachable path.
- The tempting `2/2` `best_alt_mapping=swapped_axes_512_control` hint is still diagnostic-only. A bounded runtime BFS from the promoted nearest-standable seed (`1/18`, world `(9472,6400,768)`) reached thousands of admitted cells without ever entering the `change_cube` zone, so do not promote that hint into canonical locomotion or viewer seeding without new checked-in evidence.
- The checked-in `2/2` trigger proof is now a bounded raw-start zone-recovery nudge, not a new floor-mapping admission. The baked hero start at `(9724,1024,782)` sits 4 world units west of the `change_cube` zone, so the canonical runtime path nudges east by a small fixed recovery step only when that move enters a previously absent zone; do not misread that seam as proof that general raw-invalid movement or swapped-axis floor mapping is solved.
- The guarded `19/19` object-`2` reward loop is not an idle-only path. The later `LM_SWIF` / `LM_SNIF` branch only stabilizes after the object leaves the pre-hit prefix, so tests that want the reward cycle need one priming tick to reach label `4` and then an explicit nonzero `last_hit_by` to stay on the later branch; do not assume the idle lane alone will ever emit bonus events.
- "Sendell's Ball" is now a multi-room proof family in this repo, not shorthand for raw scene `19`. Keep Tavern (`5`), Sewers (`19`), Treasure room (`20`), and the actual Sendell's Ball room (`36`) as distinct save/profile targets; otherwise it is easy to mistake the guarded sewer reward loop for the story-critical Sendell's Ball room behavior.
- `COMMON.H` is not sufficient proof for track or life opcode semantics. Let checked-in decoder behavior, asset-backed probes, and recovered source behavior beat header names or comments.
- Preserved legacy specs such as LBArchitect are useful structure guides, not byte-perfect numeric or indexing oracles. Let current asset-backed probes and tests win for exact values.
