# Generated Task Briefing

<!--
GENERATED FILE. DO NOT EDIT.

Generated by:
  python tools/codex_memory.py briefing --task 'implement stronger memory briefing retrieval' --path tools/codex_memory.py --tag codex-memory --lesson decision.task-briefing-is-derived

Source hash:
  sha256:603337f5fd9e3df8ee2e2e7a23661a70cf260a737d443958a1e7c7070ebe79a9
-->

## Task

implement stronger memory briefing retrieval

## Project Brief

# Project Brief

## Purpose

Reverse-engineering and port-planning workspace for Twinsen's Little Big Adventure 2. The canonical implementation path moves through the Zig `port/` workspace, while original-runtime evidence tooling remains a separate supporting track.

## Repo Map

- `docs/`: checked-in research, plans, subsystem memory packs, and reference memos
- `tools/`: repo-local utilities for memory, phase0, and MBN corpus work
- `tools/life_trace/`: original-runtime evidence, debug-control probes, and save-driven automation helpers
- `reference/`: imported classic source, preserved tooling, and external source material
- `work/`: rebuildable generated outputs and extracted assets
- `port/`: canonical modern port implementation

## Canonical Sources

- `docs/LBA2_ZIG_PORT_PLAN.md`
- `docs/codex_memory/current_focus.md`
- `port/README.md`

## Invariants

- One canonical current-state codepath per subsystem.
- One canonical investigative stack for narrowing runtime gaps: `inspect-room-intelligence`, `cdb-agent`, and `ghb`.
- Repo-owned generated metadata artifacts beat legacy external text exports on canonical tool paths.
- Codex memory lives only under `docs/code

[excerpt trimmed]

## Current Focus

# Current Focus

## Current Priorities

- Phase 5 remains active; the `0013` key-door-cellar slice is closed.
- Keep `codex-memory-v2`, guarded loads, promotion packets, and additive validation canonical.
- Preserve life/room/debugger boundaries.

## Active Streams

- Guarded loads: `19/19`, `2/2`, `11/10`, `187/187`; `44/2` rejects.
- `0013` is closed: proof doc, fixture, and promotion packet cover save load, key pickup, key-consume door, cellar entry, and Down-return.
- Runtime/gameplay seam widening requires `docs/promotion_packets/`; `tools/validate_promotion_packets.py` enforces the `canonical_runtime` status gate.
- `inspect-room-transitions 2 1/2 0 --json` is runtime-aware for `0013`; read runtime fields over decoded rows.
- Original-runtime launch uses mixed-mode CD from Alcohol `E:`; WinMM proxy is opt-in instrumentation only.
- Named saves use globals + pose + `SaveGame(TRUE)`; loads use `LBA2.EXE SAVE\<name>.LBA` with autosave guard.

## Blocked Items

- `2/2` is not solved interior handoff/locomotion semantics.
- `187/187` gameplay/transition beyond startup seed is unproved.
- `3/3` remains decode-only/live-negative for gameplay.
- `inside dark monk1.LBA` proves only cube `185` save with raw scene entry `187`.
- Room `36/36` page 2 is renderer pagination, not save/load behavior.
- Wall mapping is deferred.
- `inspect-room 219 219 --json` still fails; fragment-zone CLI reports aligned-origin candidates.

## Next Actions

- For `0013`, maintain only: assert proof doc, fixture, promotion packet, and runtime-aware `inspect-room-transitions`.
- Before reopening `187/187`, require fresh mixed-mode CD proof with life-loss watcher enabled and target-zone membership or `NewCube/NewPos`.
- Keep promotion packets current when widening Phase 5 seams.
- De

[excerpt trimmed]

## Query

- Paths: `tools/codex_memory.py`
- Subsystems: `architecture`
- Tags: `codex-memory`
- Lessons: `decision.task-briefing-is-derived`

## Relevant Lessons

### decision.task-briefing-is-derived

Status: active
Confidence: high
Last verified: 2026-04-29
Selected because: explicit lesson `decision.task-briefing-is-derived`; tag match `codex-memory`; exact path match `tools/codex_memory.py`; id token match `briefing`; tag token match `memory`; body token match `briefing`, `memory`, `retrieval`; global/canonical decision

`docs/codex_memory/generated/task_briefing.md` is a reproducible task lens, not
canonical truth. The canonical startup path remains `project_brief.md`,
`current_focus.md`, selected subsystem packs, and typed history through
`tools/codex_memory.py context`. Use explicit `--path`, `--subsystem`, `--tag`,
or `--lesson` briefing inputs when the task scope is known, because task prose
alone is only a retrieval hint.

### trap.current-focus-heading-is-schema

Status: active
Confidence: high
Last verified: 2026-04-29
Selected because: tag match `codex-memory`; exact path match `tools/codex_memory.py`; tag token match `memory`; body token match `memory`

`docs/codex_memory/current_focus.md` section headings are part of the validated
memory contract. Do not rename required headings such as `## Blocked Items`
unless `tools/codex_memory.py` and its tests change in the same diff.

### trap.lessons-are-not-logs

Status: active
Confidence: high
Last verified: 2026-04-29
Selected because: tag match `codex-memory`; tag token match `memory`

`lessons.md` is curated operational truth. Do not append to it just because work
finished; add a lesson only when it states reusable future behavior, a durable
trap, a decision, or an invariant that remains useful after the immediate issue
is closed.


## Relevant Open Issues

- `python3 tools/codex_memory.py context --include-history ...` still hides excluded durable history by default. If a task explicitly needs `sidequest/`, `LM_TASKS/`, LM/debug streams, or other noncanonical history, add `--include-excluded-history` instead of assuming the records disappeared.
  Selected because: token match `codex`, `memory`, `tools`
- `tools/codex_memory.py` validates the whole v2 tree, including subsystem-pack byte budgets, before it appends history. If an `add-*` command fails unexpectedly, check pack budgets and JSONL canonicalization, not just the new record.
  Selected because: token match `codex`, `memory`, `tools`
- Memory history lookup now treats repo-relative `evidence_refs` as real retrieval edges too: exact and prefix path queries can match them, and subsystem-scoped pickup can infer ownership from them. If a record is still missing, check the query path string and normalization before assuming evidence-only history is ignored.
  Selected because: token match `memory`, `retrieval`
- On this Windows checkout, WSL/Bash may also miss `git` while native PowerShell Git works. Run Codex memory and repo-state commands from the provided PowerShell shell before diagnosing the checkout or memory tooling.
  Selected because: token match `codex`, `memory`
- The canonical current-state owners are `docs/LBA2_ZIG_PORT_PLAN.md`, `docs/codex_memory/current_focus.md`, and the relevant subsystem packs. Do not recreate a separate prompt-brief owner for the next slice.
  Selected because: token match `codex`, `memory`
- The checked-in memory files can lag behind the live worktree. Verify the code and `git status` before treating `docs/codex_memory/current_focus.md` or any subsystem pack as a statement about dirtiness, generated outputs, or active experiments.
  Selected because: token match `codex`, `memory`
- `docs/codex_memory/*.jsonl` are durable append-only history, not editable ledgers. If you change an old `task_events.jsonl` row in place, validation now rejects the file relative to `HEAD`; restore the historical row and append a new event instead.
  Selected because: token match `codex`, `memory`
- if reusable lesson: docs\codex_memory\lessons.md
  Selected because: token match `codex`, `memory`
- A `.codex/worktrees/...` checkout may not mirror bulky original-runtime artifacts under `work/`. If a helper needs extracted `LBA2.EXE` assets, verify the canonical path under `D:\repos\reverse\littlebigreversing\work\...` instead of assuming the current worktree carries the same files.
  Selected because: token match `codex`
- Do not outsource save-profile visual validation to automation or the operator alone. Load the save, capture the loaded-scene screenshot, and have Codex compare it against the profile's screenshot-verifiable cues here before trusting the save as proof input.
  Selected because: token match `codex`
- For original-runtime `0013` save creation, do not assume teleporting the live hero object and calling `CurrentSaveGame()` creates a reloadable save at that object position. On `2026-04-26`, the corrected direct-save lane for `scene2-bg1-key-midpoint-facing-key.LBA` required hero pose plus `SceneStartX/Y/Z` and `StartXCube/Y/Z`; decompressed context then showed `SceneStart=(3478,2048,4772)`, `StartCube=(6,9,9)`, and direct `LoadGame()` memory returned Twinsen at `(3478,2048,4772)` facing beta `3584`. Do not promote a save without a reload memory snapshot showing the new coordinates.
  Selected because: token match `memory`
- Historical LM/debug notes and older typed history can still mention `scripts/trace-life.ps1`, but the canonical original-runtime tracer entrypoint is now `py -3 .\tools\life_trace\trace_life.py`. Do not reintroduce a wrapper layer unless the Python CLI is missing a required current-state behavior.
  Selected because: token match `tools`

## Recent Events

- 2026-04-29T06:31:54Z: codex-memory-briefing-retrieval - Strengthened generated task briefings with explicit path, subsystem, tag, and lesson filters plus relevance reasons and byte budgets.
  Selected because: affected path `tools/codex_memory.py`; evidence path `tools/codex_memory.py`; path overlap `tools/codex_memory.py`; subsystem match `architecture`; task token match `briefing`, `memory`, `retrieval`
- 2026-04-29T05:53:41Z: codex-memory-lessons-layer - Added the curated lessons layer plus deterministic generated memory index, stale report, and task briefing commands.
  Selected because: affected path `tools/codex_memory.py`; evidence path `tools/codex_memory.py`; path overlap `tools/codex_memory.py`; subsystem match `architecture`; task token match `briefing`, `memory`
- 2026-04-17T00:16:24Z: memory - Hardened codex-memory history so JSONL files reject non-append edits relative to HEAD, repaired rewritten task-event rows, and documented a stronger task-thread model.
  Selected because: affected path `tools/codex_memory.py`; evidence path `tools/codex_memory.py`; path overlap `tools/codex_memory.py`; subsystem match `architecture`; task token match `memory`, `stronger`
- 2026-04-07T07:32:28Z: codex-memory-maintenance - Memory history lookup now treats evidence_refs as retrieval edges for subsystem inference and exact/prefix path matches.
  Selected because: affected path `tools/codex_memory.py`; evidence path `tools/codex_memory.py`; path overlap `tools/codex_memory.py`; subsystem match `architecture`; task token match `memory`, `retrieval`
- 2026-04-16T05:41:10Z: repo_review - Ran a multi-agent repo review, wrote a consolidated swarm report under D:/repos/reverse/report, and triaged the highest-leverage confirmed code, tooling, and documentation risks.
  Selected because: affected path `tools/codex_memory.py`; evidence path `tools/codex_memory.py`; path overlap `tools/codex_memory.py`; subsystem match `architecture`; task token match `memory`

## Retrieval Warnings

- No selected lesson, issue, or event matched `implement`.

## Required Checks

- Run `python tools/codex_memory.py validate`.
- Run relevant project tests for touched areas.
- If runtime/gameplay seams are widened, validate promotion packets.
- Do not treat generated task briefing as canonical truth.
