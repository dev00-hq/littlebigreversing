# Generated Task Briefing

<!--
GENERATED FILE. DO NOT EDIT.

Generated by:
  python tools/codex_memory.py briefing --task 'game-drive runner'

Source hash:
  sha256:60d6b099e22e3e801a1d9c65a66578106c613e8c83c149b603b056bd965e888f
-->

## Task

game-drive runner

## Project Brief

# Project Brief

## Purpose

Reverse-engineering and port-planning workspace for Twinsen's Little Big Adventure 2. The canonical implementation path moves through the Zig `port/` workspace, while original-runtime evidence tooling remains a separate supporting track.

LBA2 gameplay is a story-gated, hub-based action-adventure: world state, inventory, dialogue, location access, actor state, and quest flags unlock affordances and change behavior. Do not model the campaign as a linear room graph.

## Repo Map

- `docs/`: checked-in research, plans, subsystem memory packs, and reference memos
- `tools/`: repo-local utilities for memory, phase0, and MBN corpus work
- `tools/life_trace/`: original-runtime evidence, debug-control probes, and save-driven automation helpers
- `reference/`: imported classic source, preserved tooling, and external source material
- `work/`: rebuildable generated outputs and extracted assets
- `port/`: canonical modern port implementation

## Canonical Sources

- `docs/LBA2_ZIG_PORT_PLAN.md`
- `docs/codex_memory/current_focus.md`
- `port/README.md`

## Invariants

- One canonical current-state codepath per subsystem.
- One canonical investigative

[excerpt trimmed]

## Current Focus

# Current Focus

## Current Priorities

- Phase 5 remains active; the `0013` key-door-cellar slice is closed.
- Keep memory v2, guarded loads, promotion packets, and additive validation canonical.
- Preserve life/room/debugger boundaries.

## Active Streams

- Guarded loads: `19/19`, `2/2`, `11/10`, `187/187`; `44/2` rejects.
- `0013` is closed: packet covers save load, key pickup, door consume, cellar entry, and Down-return.
- Promoted contracts: Magic Ball pickup/throw/bounce/Tralu damage/Emerald Moon switches/multi-family levers; text interaction affordance-family owner split.
- Magic Ball switch activation is promoted only for Emerald Moon object `3` and corrected object `4`; object `2` remains unpromoted.
- Magic Ball lever activation is promoted only as `magic_ball_lever_activation_multi_family`; Warehouse is the negative control.
- Broader Magic-ball damage, generic switches/levers, remote pickup, enemy vulnerability, and collision geometry remain unpromoted.
- Runtime/gameplay seam widening requires `docs/promotion_packets/` and promotion validation.
- `inspect-room-transitions 2 1/2 0 --json` is runtime-aware for `0013`; read runtime fields over decoded rows.
- Original-runtime launch uses mixed-mode CD from Alcohol `E:`; WinMM proxy is opt-in only.
- Named saves use globals + pose + `SaveGame(TRUE)`; loads use `LBA2.EXE SAVE\<name>.LBA` with autosave guard.

## Blocked Items

- `2/2` is not solved interior handoff/locomotion semantics.
- `187/187` gameplay/transition beyond startup seed is unproved.
- `3/3` zones `1` and `8` remain live-negative for gameplay.
- `inside dark monk1.LBA` proves only cube `185` save with raw scene entry `187`.
- Room `36/36` page 2 is renderer pagination, not save/load behavior.
- Generic dialogue branching, Dino-Fl

[excerpt trimmed]

## Query

- Paths: none
- Subsystems: none
- Tags: none
- Lessons: none

## Relevant Lessons

### decision.task-briefing-is-derived

Status: active
Confidence: high
Last verified: 2026-04-29
Selected because: global/canonical decision

`docs/codex_memory/generated/task_briefing.md` is a reproducible task lens, not
canonical truth. The canonical startup path remains `project_brief.md`,
`current_focus.md`, selected subsystem packs, and typed history through
`tools/codex_memory.py context`. Use explicit `--path`, `--subsystem`, `--tag`,
or `--lesson` briefing inputs when the task scope is known, because task prose
alone is only a retrieval hint.

### trap.live-directdraw-window-capture-can-be-unavailable

Status: active
Confidence: high
Last verified: 2026-05-02
Selected because: body token match `drive`, `game`

Unattended original-runtime runs can load the correct save and expose valid
runtime globals while live window screenshot capture returns a black DirectDraw
surface or cannot access the desktop. Do not silently treat that as visual
proof. Game-drive checkpoints must declare their visual source explicitly. A
prepared named-save checkpoint may use `save_embedded_preview` paired with live
runtime globals, but teleport/direct-pose checkpoints must use
`live_window_capture` and remain blocked if live screenshots are unavailable.

### fact.original-runtime-throw-input-is-hold-release

Status: active
Confidence: high
Last verified: 2026-04-29
Selected because: body token match `drive`

Magic Ball automation must drive weapon input as a hold-then-release action,
not a tap. Source `OBJECT.CPP` keeps `I_THROW` active while aiming and switches
`MAGIC_BALL_LANCEE` to `MAGIC_BALL_LACHEE` only after release; default source
input is `ALT`, while this runtime profile maps weapon use to `.`. On the
`throw-ball.LBA` proof save at pose `(5071,1024,1820,beta=3709)`, a `.` hold of
`0.61s` failed, `0.62s` succeeded for Aggressive, and `0.63s` succeeded for
Normal, Sporty, and Discreet. Future automated gameplay probes should hold the
weapon key for at least `0.75s` before release unless a task is specifically
measuring the threshold.

### trap.original-runtime-autosave-overrides-named-save

Status: active
Confidence: high
Last verified: 2026-04-29
Selected because: body token match `game`

Original-runtime named-save probes must hide or remove active `autosave.lba`
before launch and preserve any generated autosave afterward; otherwise the game
can silently load the wrong state. Treat a clean named-save load as unproved
unless the save directory has no active `autosave.lba` at launch and no generated
`autosave.lba` is left active after the probe exits.


## Relevant Open Issues

- Game-drive runner behavior-mode and broader teleport proof remain blocked. The
  Selected because: token match `drive`, `game`, `runner`
- Original-runtime DxWnd runs must mount the mixed-mode CUE in the existing Alcohol 120% virtual drive `E:`, not Windows' separate virtual DVD drive. The current canonical media path is `work/runtime_media/lba2_mixed_mode/LBA2_TWINSEN_mixed.cue`; do not treat drive letter, `MediaLoaded=True`, volume label, or `E:\LBA2\VIDEO\VIDEO.HQR` existence as sufficient CD-gate proof without an actual game/menu or runtime proof.
  Selected because: token match `drive`, `game`
- DXWnd-managed windowed startup is acceptable on the original-runtime proof lanes, but the canonical launch path is now direct save launch via `LBA2.EXE <save>.LBA` followed by one `Enter` on the Adeline splash. Do not route Tavern, Scene11, or room-36 probes back through the old staged single-slot `Load Game` harness.
  Selected because: token match `game`
- Direct CDB hardware-breakpoint capture on `NbLittleKeys` is proof-capable but risky for manual live loops. The `secret-room-key-counter-watch-30492-manual-extra-dump` run captured the key extra payload, then left the game in an application-error state after the breakpoint command quit; prefer Frida/read-only probes unless a one-shot CDB write stop is specifically needed.
  Selected because: token match `game`
- LBA2 window screenshots can have invalid alpha: several valid RGBA captures stored the real frame in RGB while marking the scene transparent. Phone-share must publish `--kind game` captures from RGB and ignore alpha, otherwise the WebP can flatten to a blank white image even when the original PNG visually contains the scene.
  Selected because: token match `game`
- On the direct-save launch path, the black EA logo can look like a stable lit splash to coarse frame-signature polling. On `2026-04-21`, `waypoint_step_probe.py --launch-save .\work\saves\0013-weapon.LBA` initially sent `Enter` on the EA logo, captured Adeline-logo screenshots, and returned all-zero hero fields. Keep the Adeline gate bright enough to reject the EA logo before pressing `Enter`; a valid smoke must show in-game screenshots and nonzero Frida pose fields.
  Selected because: token match `game`
- The canonical original-runtime `SAVE` baseline is no longer part of controlled proof-lane startup. For Tavern, Scene11, room-36, and related probes, launch the game directly with the desired `.lba` from `work/saves` and let the Adeline splash hand off into the save. Do not reintroduce staged-save folder surgery or menu-driving as a fallback.
  Selected because: token match `game`
- The guarded `2/2` public house exit is not a blocked interior-room handoff. Original-runtime slot-`0` tracing from the playable New Game lane captured a discontinuous jump from house coordinates to an exterior landing near `(18434,250,5702)`, so do not keep modeling `destination_cube=0` there as an interior same-index room candidate or as a post-load-adjustment problem.
  Selected because: token match `game`
- The magic-ball pickup proof and `magic_ball_pickup` port contract cover only `ListVarGame[FLAG_BALLE_MAGIQUE] 0 -> 1` / game var `1`. They do not prove New Game equivalence, Sendell portrait/dialog flags, magic refill behavior, inventory-menu rendering, or throwing/usability. The first manual run's final screenshot was focus-contaminated after the operator switched windows; the repeatable launch run `work/live_proofs/phase5_magic_ball_launch_20260429_r2` has clean LBA2-focused screenshots and should be the preferred visual evidence.
  Selected because: token match `game`
- The tiny top-level Zig test roots under `port/src/` are intentional. If you point `build.zig` test shards directly at files under `src/tools/` or `src/game_data/...`, Zig can fail with `import of file outside module path` when those tests reach back across the `src/` tree, so keep the wrapper roots unless you also redesign the module layout.
  Selected because: token match `game`

## Recent Events

- 2026-05-02T17:01:17Z: game-drive-runner - Added capability ladder: load, visual gate, rotation, translation, Magic Ball, dialogue, and direct-pose pass; behavior-cycle remains blocked.
  Selected because: task token match `drive`, `game`, `runner`
- 2026-05-02T08:01:00Z: gameplay-automation - Added runtime polling around game-drive key dispatch and recorded the current unattended limitation: the five-save run still passes load and visual gates, but watched action fields did not pulse during declared key actions.
  Selected because: task token match `drive`, `game`, `runner`
- 2026-05-02T07:55:00Z: gameplay-automation - Implemented game-drive runner and proved five prepared named saves with autosave guarding, live runtime global checks, codex-exec visual classification from explicit save-embedded preview sources, and declared action dispatch.
  Selected because: task token match `drive`, `game`, `runner`
- 2026-05-02T07:33:36Z: gameplay-automation - Verified the game-drive visual checkpoint path with a real codex exec image-classification run against the Emerald Moon middle-switch screenshot.
  Selected because: task token match `drive`, `game`, `runner`
- 2026-05-02T07:30:43Z: gameplay-automation - Added game-drive checkpoint contract for direct-pose visual gating, including codex-exec visual response validation with required summary grounding.
  Selected because: task token match `drive`, `game`, `runner`

## Retrieval Warnings

- None.

## Required Checks

- Run `python tools/codex_memory.py validate`.
- Run relevant project tests for touched areas.
- If runtime/gameplay seams are widened, validate promotion packets.
- Do not treat generated task briefing as canonical truth.
