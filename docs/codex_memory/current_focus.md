# Current Focus

## Current Priorities

- Keep the topology-first `codex-memory-v2` tree canonical and small.
- Continue the decode-first port path beyond the height-aware interior composition debug view, the canonical `2/2` zero-fragment probe, the landed synthetic brick-index overlays, the actual `BRK`-backed swatch previews, the deterministic fragment comparison panel, and the new selected-cell detail strip for the checked-in `11/10` pair.
- Keep the checked-in fragment-bearing interior evidence anchored to `SCENE.HQR[11]` plus `LBA_BKG.HQR[10]`, not guessed pairings.
- Keep unsupported life switch-family handling blocked until checked-in evidence changes or the product boundary rejects it explicitly.

## Active Streams

- Scene and background inspection plus the height-aware interior composition debug viewer, zero-fragment `2/2` probe, fragment-bearing `11/10` GRM evidence path, synthetic brick-index overlays, actual `BRK`-backed swatch previews, and the new viewer-local fragment comparison panel / focus highlight / selected-cell detail strip as viewer-prep surfaces.
- MBN corpus and phase0 evidence maintenance.
- Windows-first port validation with Linux limited to docs and source-oriented work.

## Blocked Items

- Scene-surface life integration is blocked on `LM_DEFAULT` and `LM_END_SWITCH`.
- Canonical `zig build test` and `zig build run` still depend on the extracted asset tree and repo-local Windows SDL2 layout.

## Next Actions

- Build future viewer work on the viewer-local composition snapshot, height cues, and schematic path, not on a new handoff layer.
- Build future viewer-local refinement on the landed `11/10` fragment comparison panel, focus highlight, and selected-cell detail strip rather than replacing the viewer-local snapshot path, and keep it away from gameplay, life binding, exterior loading, or a full room-art renderer.
- Keep future life work scoped to explicit evidence probes or deliberate rejection of unsupported switch-family opcodes.
- Update subsystem packs and typed records instead of reviving timeline-heavy summary docs.

## Relevant Subsystem Packs

- architecture
- assets
- mbn_corpus
- phase0_baseline
- scene_decode
- backgrounds
- life_scripts
- platform_windows
- platform_linux
